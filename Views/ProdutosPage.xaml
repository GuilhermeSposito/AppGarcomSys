<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="AppGarcomSys.Views.ProdutosPage"
             Title="Produtos">
    <ScrollView>
        <VerticalStackLayout x:Name="LayoutContainer">
            <VerticalStackLayout x:Name="LayoutDePesquisa">
                
            </VerticalStackLayout>
            
            <Grid x:Name="GridDeProdutos" RowSpacing="10" ColumnDefinitions="5*,5*" Margin="10" ColumnSpacing="5" Padding="5">

                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
            </Grid>
        </VerticalStackLayout>
    </ScrollView>
</ContentPage>